<h3><b>Grid</b></h3>
<h4>- Use</h4>
<h4>The grid is composed of the global grid + the grid item</h4>
<ul>
    <li>
        <b>with twig extension:</b>
            <pre>
{{ "{{ alamanach_grid($content, $framework, $class = [], $attributes = [], $tag = null, $link = null) }}"|e }}</pre>
    </li>
    <li>
        <b>with twig embed:</b>
            <pre>
{{ "{% embed 'AlmanachBundle:bricks/' ~ framework ~ ':_grid.html.twig' with {content: content, framework: framework, ... } only %}"|e }}
{{ "    {% block content %}"|e }}
{{ "        {# content #}"|e }}
{{ "    {% endblock %}"|e }}
{{ "{% endembed %}"|e }}</pre>
    </li>
</ul>
<br><h4>- parameters:</h4>
<ul>
    <li>
        <b>content</b>: content of your grid.
    </li>
    <li>
        <b>framework</b>: framework you are using.
        <ul>
            <li>bootstrap</li>
            <li>foundation</li>
        </ul>
    </li>
    <li>
        <b>classGridItem</b>: classGridItem of your grid item.
        <ul>
            <li></li>
            <li></li>
            <li></li>
        </ul>
    </li>
</ul>
<h4>- example</h4>

<ul style="list-style-type: none">
    <li>
        {% embed 'AlmanachBundle:bricks/' ~ framework ~ ':_grid.html.twig' with {framework: framework} only %}
            {% block content %}
                {% embed 'AlmanachBundle:bricks/' ~ framework ~ ':_gridItem.html.twig' with {framework: framework, classGridItem: 'lg2'} only %}
                    {% block content %}
                        Div 1
                    {% endblock %}
                {% endembed %}
                {% embed 'AlmanachBundle:bricks/' ~ framework ~ ':_gridItem.html.twig' with {framework: framework, classGridItem: 'lg2'} only %}
                    {% block content %}
                        Div 2
                    {% endblock %}
                {% endembed %}
                {% embed 'AlmanachBundle:bricks/' ~ framework ~ ':_gridItem.html.twig' with {framework: framework, classGridItem: 'lg2'} only %}
                    {% block content %}
                        Div 3
                    {% endblock %}
                {% endembed %}
            {% endblock %}
        {% endembed %}
    </li>
    <li>
        {{ almanach_grid([
            almanach_gridItem('Div 1', framework, 'lg4'),
            almanach_gridItem('Div 1', framework, 'lg4'),
            almanach_gridItem('Div 1', framework, 'lg4'),
        ],
        framework
        ) }}
    </li>
</ul>
{#almanach_gridItem('Div 3', framework, ['xs4', 'sm4', 'md4', 'lg4']),#}
