<h2>Alignment Text</h2>
<h4>+ Use</h4>
<ul>
    <li>
        <b>with twig extension:</b>
            <pre>
{{ "{{ alamanach_alignmentText($content, $framework, $alignment = 'default', $class = [], $attributes = [], $tag = null, $link = null) }}"|e }}</pre>
    </li>
    <li>
        <b>with twig embed:</b>
            <pre>
{{ "{% embed 'AlmanachBundle:bricks/' ~ framework ~ ':_alignmentText.html.twig' with {content: content, framework: framework, ... } only %}"|e }}
{{ "    {% block content %}"|e }}
{{ "        {# content #}"|e }}
{{ "    {% endblock %}"|e }}
{{ "{% endembed %}"|e }}</pre>
    </li>
</ul>
<br><h4>+ parameters:</h4>
<ul>
    <li>
        <b>content</b>: content of your alignment text.
    </li>
    <li>
        <b>framework</b>: framework you are using.
        <ul>
            <li>bootstrap</li>
            <li>foundation</li>
        </ul>
    </li>
    <li>
        <b>alignment</b>: alignment of your text.
        <ul>
            <li>left</li>
            <li>center</li>
            <li>right</li>
            <li>justify</li>
        </ul>
    </li>
</ul>
<h4>+ example</h4>
<ul style="list-style-type: none">
    <li>
        {{ almanach_alignmentText('Your "left" alignment text !', framework, 'left') }}
    </li>
    <li>
        {{ almanach_alignmentText('Your "center" alignment text !', framework, 'center') }}
    </li>
    <li>
        {% embed 'AlmanachBundle:bricks/' ~ framework ~ ':_alignmentText.html.twig' with {framework: framework, alignment: 'right' } only %}
            {% block content %}
                Your "right" alignment text !
            {% endblock %}
        {% endembed %}
    </li>
    <li>
        {% embed 'AlmanachBundle:bricks/' ~ framework ~ ':_alignmentText.html.twig' with {framework: framework, alignment: 'justify' } only %}
            {% block content %}
                Your "justify" alignment text !
            {% endblock %}
        {% endembed %}
    </li>
</ul>
