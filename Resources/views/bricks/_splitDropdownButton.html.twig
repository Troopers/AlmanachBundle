{##
 # ALLOWED PARAMETERS
 # theme : required - string in [default, error, information, link, primary, secondary, success, warning]
 # size : required - string in [extraSmall, small, medium, default, large, expanded]
 # state : required - string in [default, active, disabled]
 # border : required - string in [default, round, radius]
 # framework: required - string in [bootstrap, foundation]
 # list: required - string in [{link: '#1', label: 'link 1'}, {link: '#2', label: 'link 2'}, {link: '#3', label: 'link 3'}]
 # attributes: optional - object, example: {role: 'test', 'data-target': 'body'}
 # class: optional - array contain classes, example: ['hide']
 # tag: optional - string, example "button"
 # link: optional - if tag is "a", you can pass link
 #}

{#Include du markup de la feature "SplitDropdown button"#}
{% for _size, value in config_splitDropdownButton.size %}
    {% for _theme, value in config_splitDropdownButton.theme %}
        {% for _state, value in config_splitDropdownButton.state %}
            {% for _border, value in config_splitDropdownButton.border %}
        {#{% embed "AlmanachBundle:bricks:#{framework}/_splitDropdownButton.html.twig" with {framework: framework, theme: _theme, size: _size, state: _state, border: _border, list: list} only %}#}
            {#{% block content %}{{ type }} {{ size }}{% endblock %}#}
        {#{% endembed %}#}
        {{ almanach_splitDropdownButton(_theme ~ ' ' ~ _size, framework, _theme, _size, _state, _border) }}
            {% endfor %}
        {% endfor %}
    {% endfor %}
    <br><br>
{% endfor %}
