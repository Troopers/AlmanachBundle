{% set almanach = attribute(almanach, framework) %}
{% set classes = classes is defined ? classes : [] %}
{% set attributes = attributes is defined ? attributes : {} %}

{% set classes = classes|merge([
    'tab-pane',
    attribute(almanach.tabContent.state, state is defined ? state : 'default')
]) %}

{% set attributes = id is defined ? attributes|merge({id: id}) : attributes %}

{% set attributes = attributes|merge({'role': 'tabpanel'}) %}

{{ almanach_alignmentText(content, framework, 'default', classes, attributes) }}
