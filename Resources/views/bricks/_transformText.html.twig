{##
 # ALLOWED PARAMETERS
 # transform : [Lower, Upper, Capital]
 #}

{% set transforms = ['Lower', 'Upper', 'Capital'] %}

{% for _transform in transforms %}
    {% embed "AlmanachBundle:bricks:#{framework}/_transformText.html.twig" with {framework: framework, transform: _transform} only %}
        {% block content %}{{ transform }}{% endblock %}
    {% endembed %}
{% endfor %}
