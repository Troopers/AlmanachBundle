{##
 # ALLOWED PARAMETERS
 # theme : required - string in [default, error, information, link, primary, secondary, success, warning, active, disabled]
 # size : required - string in [extraSmall, small, medium, default, large, round, radius, expanded]
 # framework: required - string in [bootstrap, foundation]
 # list: required - string in [{link: '#1', label: 'link 1'}, {link: '#2', label: 'link 2'}, {link: '#3', label: 'link 3'}]
 # attributes: optional - object, example: {role: 'test', 'data-target': 'body'}
 # class: optional - array contain classes, example: ['active', 'hidden']
 # tag: optional - string, example "a"
 # link: optional - if tag is "a", you can pass link
 #}

{#{% set list = [{link: '#1', label: 'link 1'}, {link: '#2', label: 'link 2'}, {link: '#3', label: 'link 3'}] %}#}

{#Ici vous pouvez rajouter un ou plusieurs attributs dans le tableau suivant, exemple :#}
{#"id": "test-id-button",#}
{% set attributes = {
} %}

{#Ici vous pouvez rajouter une ou plusieurs classes dans le tableau suivant, exemple :#}
{#"test-class",#}
{% set class = {
} %}

{#Include du markup de la feature "Dropdown button"#}
{% for _size, value in config_dropdownButton.size %}
    {% for _theme, value in config_dropdownButton.theme %}
        {% for _state, value in config_dropdownButton.state %}
            {% for _border, value in config_dropdownButton.border %}
                {#Liste des paramètres à envoyer pour gérer la feature "dropdown buttons" : #}
                {#{% embed "AlmanachBundle:bricks:#{framework}/_dropdownButton.html.twig" with {framework: framework, theme: _theme, size: _size, state: _state, border: _border, list: list, attributes: attributes, class: class, tag: 'button'} only %}#}
                    {#{% block content %}{{ theme }} {{ size }}{% endblock %}#}
                {#{% endembed %}#}
                {{ almanach_dropdownButton(_theme ~ ' ' ~ _size, framework, _theme, _size, _state, _border) }}
            {% endfor %}
        {% endfor %}
    {% endfor %}
    <br><br>
{% endfor %}
