{% set almanach = attribute(almanach, framework) %}
{% set class = class is defined ? class : [] %}
{% set attr = attr is defined ? attr : {} %}
{% set tag = tag|default('div') %}

{% set classComponent = class|merge([
    'content',
    attribute(almanach.tabContent.state, state is defined ? state : 'default')
]) %}

{% set attr = id is defined ? attr|merge({id: id}) : attr %}

{#{% set attr = attr|merge({#}
    {#id: id#}
{#}) %}#}

{#{{ almanach_alignmentText(content, framework, {#}
        {#class: [#}
            {#'content'#}
        {#]#}
    {#})#}
{#}}#}

<{{ tag }}
    {% include 'AlmanachBundle:bricks/global:attributes.html.twig' with {
        class: classComponent,
        attr: attr
    } only %}
>
    {% block content %}
        {{ content|raw }}
    {% endblock %}
</{{ tag }}>

