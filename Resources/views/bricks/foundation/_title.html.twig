{% set almanach = attribute(almanach, framework) %}

<{{ attribute(almanach.title.size, size) }}>
    {% block content %}
        {{ content|raw }}
    {% endblock %}
</{{ attribute(almanach.title.size, size) }}>