{% set almanach = attribute(almanach, framework) %}

<{{ attribute(almanach.inlineText.inline, inline) }}>
    {% block content %}
        {{ content|raw }}
    {% endblock %}
</{{ attribute(almanach.inlineText.inline, inline) }}>
